!!!
html
  head
    block head
      meta(charset='utf-8')
      title PlugMap
      block styles
        link(href='/ui.css',rel='stylesheet')
        link(href='//fonts.googleapis.com/css?family=Russo+One',rel='stylesheet')
      block scripts
      meta(name="viewport",
        content="width=device-width, initial-scale=1, maximum-scale=1")
      link(href='/favicon.png',rel='icon')
  body
    header
      h1: a(href='/') PlugMap
      .headpanel
        block headpanel
          block headbutton
            if username
              a(href='/submit').button.green Add a plug
            else
              a(href='/login').button.green Log in
          if username
            a(href='/user/'+username)
              img(src='//gravatar.com/avatar/'+emailMD5).current-user-avatar
    block content
      main
        block main
